#include <stdio.h>

int *findmid(int a[],int n)
  {
    return &a[n/2];
  }

int main()
{
    int a[] = {1,2,4,5,6,7,8,9,10};
    int n = sizeof(a)/sizeof(a[0]);
    int *mid = findmid(a,n);
    printf("%d", *mid);
    return 0;
}
